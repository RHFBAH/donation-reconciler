---
description: كيفية تعديل النظام ورفعه مرة أخرى على Vercel
---

# 🚀 دليل تعديل ورفع النظام

لإجراء تعديلات على النظام ورفعها على الرابط المباشر، اتبع الخطوات التالية:

### 1. إجراء التعديلات محلياً
قم بتعديل الملفات البرمجية داخل مجلد المشروع (`donation-reconciler`) باستخدام محرر الأكواد الخاص بك.

### 2. تجربة التعديلات
تأكد من أن التعديلات تعمل بشكل صحيح في بيئة التطوير:
```powershell
npm run dev
```
ثم افتح `http://localhost:3000` في متصفحك.

### 3. التحقق من بناء النظام (Build)
قبل الرفع، تأكد من عدم وجود أخطاء برمجية قد تمنع التشغيل:
// turbo
```powershell
npm run build
```

### 4. الرفع على Vercel
عندما تكون جاهزاً لنشر التعديلات على الرابط المباشر:
// turbo
```powershell
npx vercel --prod --yes
```

> [!NOTE]
> سيقوم أمر `npx vercel --prod` بتحديث الرابط المباشر (Production) مباشرة. إذا أردت التجربة أولاً دون التأثير على الرابط المباشر، استخدم `npx vercel` فقط (بدون `--prod`).
